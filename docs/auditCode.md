你现在不是在给我写一份“模块说明书”，而是在基于真实代码，为我做一次“RAG 全链路审计式讲解”。

目标不是泛泛解释 RAG 原理，而是围绕这个项目里一条最典型的 RAG 用户问题，顺着真实调用链，从入口到答案输出，把每一层的输入、输出、决策点、降级逻辑、观测点、排障方法、设计取舍全部捋清楚。

请你严格按下面要求执行，不要偷懒，不要写成空泛总结，不要脱离代码想当然。

【你的工作方式】
1. 先通读项目中与 RAG 相关的真实代码，重点关注但不限于这些文件：
   - main.py
   - graph/agent.py
   - graph/nodes.py
   - rag/service.py
   - rag/generator.py
   - rag/prompts.py
   - rag/query_analyzer.py
   - rag/retriever.py
   - rag/reranker.py
   - rag/metadata.py
   - rag/loaders.py
   - rag/cleaner.py
   - rag/splitter.py
   - rag/embeddings.py
   - rag/vector_store.py
   - rag/bm25_store.py
   - rag/indexer.py

2. 不要只按文件讲模块，要从一个“典型真实用户问题”出发来讲整条链路。
   - 优先使用仓库里已有的真实样例、测试输入、接口调用示例、业务描述中的典型问题。
   - 如果仓库中找不到明确样例，就基于项目业务域，构造一个“最可能触发 RAG 的典型问题”，并明确标注“这是推测性示例问题，不是仓库中的真实请求样例”。

3. 你最终输出的文档，不是给新人扫盲，而是给“已经看过这些模块、但还无法形成整体判断的人”看的。
   所以重点不是“这个模块做了什么”，而是：
   - 它为什么放在这里做
   - 它为什么不放在别处做
   - 如果我把它去掉、跳过、改弱，会坏在哪里
   - 当结果不对时，我应该先怀疑哪一层
   - 这一层和上下游之间到底怎么接上的

4. 整篇文档必须基于真实代码事实来写。
   - 尽量给出文件路径、类名、函数名、方法名。
   - 能定位到行号更好；如果不方便给行号，至少要给出清晰的代码入口和调用链。
   - 不确定的地方必须明确写“待确认”或“代码中未明确体现”，不要编。

【你最终要生成的文档文件】
请生成一份详细 Markdown 文档，建议保存为：
docs/rag_end_to_end_walkthrough.md

【文档必须包含的内容结构】
请严格按这个结构输出，内容要详细，但必须贴着真实代码讲。

# 1. 文档目标
先说明：这份文档不是模块百科，而是一次“沿着典型问题走完整链路”的代码审计式理解。
要明确这份文档回答的是：
- 用户问题如何从 /api/chat 进入系统
- 如何被识别为 RAG 问题
- 如何进入 query_analyzer
- query 如何生成，metadata filter 从哪里来
- retriever 为什么同时有 vector / BM25 / hybrid 或为什么没有
- reranker 在这里的职责是什么
- generator 如何拿检索结果拼 prompt 并生成答案
- 最终如何回到接口层 / SSE 返回
- 如果某一层被拿掉，线上最可能坏在哪里
- 调试时应该按什么顺序排查

# 2. 先给一张全链路 Mermaid 图
要求这张图必须基于真实代码，而不是通用 RAG 模板图。
图里至少包含：
- /api/chat 或实际入口
- graph / agent / node 层（如果存在）
- service 层
- query_analyzer
- retriever
- reranker
- generator
- 最终输出层
如果某些层不存在，就按真实代码调整。

# 3. 选定一个典型用户问题
在这里写清楚：
- 这个问题是什么
- 为什么选它作为典型问题
- 它大概率会走哪条链路
- 它为什么能代表系统的主要 RAG 路径

# 4. 从入口开始逐层走读整条链路
这是全文最核心的部分。请按真实调用顺序一层层写，不要跳。

对于每一层，都必须回答下面这些问题：

## 4.x 当前层名称
例如：
- API 入口层
- intent / graph 路由层
- RAG service 编排层
- query analyzer 层
- retriever 层
- reranker 层
- generator 层
- response 返回层

### 这一层的代码入口在哪里
写明文件、函数、类、方法。

### 这一层的输入是什么
不仅写参数名，还要写“业务语义上的输入”。
例如：
- 原始用户问题
- 聊天历史
- query analyzer 输出的 query/filter
- retriever 返回的候选 chunk
- reranker 排序后的 top chunks
- generator 需要的 prompt 片段

### 这一层的输出是什么
不仅写返回值，还要写“这个输出给下游带来了什么约束”。

### 这一层内部做了哪些关键决策
例如：
- 为什么会判定走 RAG
- query 是如何规范化 / 改写 / fallback 的
- filter 是如何提取或降级的
- 为什么同时查 vector 和 BM25，或者为什么只查其中一个
- reranker 是否裁剪了候选集
- generator 如何控制上下文拼接

### 这一层为什么必须放在这里做
不要只说“为了职责清晰”，要结合真实链路解释。
例如：
- 为什么 query 改写不放到 retriever 里
- 为什么 rerank 不放到 generator 里
- 为什么 service 只做编排，不做细节实现（如果代码是这样）
- 为什么 graph 层只负责路由，不负责检索细节（如果代码是这样）

### 如果把这一层去掉，最可能发生什么问题
必须讲“具体坏法”，不要抽象。
例如：
- 不做 query 分析会导致 filter 丢失，召回范围变散
- 不做 BM25 会导致术语命中型问题召回变差
- 不做 rerank 会导致 top chunks 噪音更大
- 不做 generator 的上下文裁剪会导致 prompt 污染或超长

### 这一层的降级 / fallback 逻辑是什么
要看真实代码，不要脑补。
例如：
- 规则优先，LLM fallback
- filter 失败则退回无 filter
- LLM 调用失败则使用 normalize query
- BM25 失败是否只走向量召回
- reranker 失败是否保留原候选顺序

### 这一层有哪些可观测点
必须尽量从代码里找：
- 日志
- 耗时统计
- 命中数
- top_k
- filter 内容
- query 内容
- chunk 数量
- 错误处理
如果代码里没有，也请明确指出“当前缺少哪些可观测点”。

### 调试这层时，第一眼应该看什么
这里请写得非常实战。
例如：
- 先看 query_analyzer 最终产出的 query/filter
- 先看 metadata 是否建对
- 先看 top_k 返回的 chunk 内容
- 先看 rerank 前后顺序差异
- 先看 generator 真实拿到的 context

# 5. 串起来讲：这一条请求从头到尾的数据是怎么流动的
这里不要按模块讲，要按“数据对象”讲。
至少把这些对象串起来：
- 原始 request
- 识别后的 intent / route
- query analyzer 产物
- retriever 产物
- reranker 产物
- generator 输入
- 最终 response

要写清楚：
- 每次对象结构发生了什么变化
- 哪些字段是在上游新增的
- 哪些字段会影响下游行为
- 哪些字段是排查问题时最关键的

# 6. 这个项目的关键设计取舍
这一节很重要，不要写空话。
请基于真实代码回答：
- 为什么这里采用规则优先还是 LLM 优先
- 为什么需要 metadata filter，或者为什么 filter 做得很轻
- 为什么需要 BM25，或者为什么可以只做向量召回
- 为什么需要 reranker，或者为什么没有上 reranker
- 为什么 service / graph / generator 要这样拆层
- 当前实现显然是在“优先稳定性 / 优先简单 / 优先召回 / 优先成本 / 优先延迟”中的哪一种
- 当前这套设计最适合什么场景，不适合什么场景

# 7. 如果我要最快建立整体认知，应该按什么顺序读这份代码
这部分请基于项目真实情况，给出一个“最短上手顺序”。
但不要只列文件名，要说明：
- 先看这个文件是为了先抓住哪条主线
- 再看那个文件是为了补哪个决策点
- 哪些文件适合最后看，因为太细或太底层

# 8. 最小排障闭环
请基于这个项目，给出一条非常实用的排障顺序，要求从最容易验证、最靠近问题表象的地方开始。
目标是回答：
“如果用户说这个 RAG 问题答得不对，我第一轮排查应该按什么顺序看？”
不要泛泛说，要贴项目代码。
例如但不限于：
- 请求是否真的走到了 RAG 路径
- query_analyzer 是否产出正确 query/filter
- retriever 是否真的召回了相关 chunk
- reranker 是否把正确 chunk 排下去了
- generator 是否拿到了正确上下文
- 最终是否是 prompt 或模型生成问题，而不是检索问题

# 9. 三个最值得做的小实验
这一节请基于当前代码提出 3 个最值得做的最小实验，用来帮助理解系统设计。
每个实验都要写：
- 改动什么
- 预期会看到什么现象
- 它能帮助理解哪一层
例如：
- 关闭规则 filter 看召回变化
- 只保留 vector 检索看术语型问题变化
- 关闭 reranker 看最终答案质量变化

# 10. 最后给一个“带着问题读代码”的版本
请输出一组高质量问题，帮助读代码时逼自己思考。
这些问题不要是“这个函数做什么”，而要像：
- 为什么 query 改写放在这里而不是 retriever 里
- filter 的失败为什么要退到无 filter
- 如果 chunk 切得更大，这个系统哪一层最先感受到副作用
- 为什么 generator 不直接自己再做一次检索
- 当前系统更怕召回不足，还是更怕召回噪音

【写作要求】
1. 整体风格不要写成教科书，也不要写成流水账。
2. 解释必须围绕真实代码和真实调用链。
3. 不要只讲“做了什么”，要重点讲“为什么这样做”“不这么做会怎样”。
4. 所有不确定的地方，明确标注“待确认”，不要脑补。
5. 能给 Mermaid 图就给 Mermaid 图。
6. 尽量多引用真实文件、函数、调用关系。
7. 如果发现项目实现和我预期不一致，要以真实代码为准，不要迎合提示词。

【额外加分项】
如果你能从代码中识别出“离线建库链路”和“在线检索链路”的边界，请在文档中额外补一小节，说明：
- 哪些是离线流程
- 哪些是在线流程
- 两者通过什么数据结构或存储衔接
- 在线问题排查时为什么不能把离线建库问题和在线检索问题混为一谈

请直接开始分析代码并生成完整 Markdown 文档内容,输出到 docs/rag_end_to_end_walkthrough.md