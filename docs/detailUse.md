你现在是我的“项目导读助手”和“代码讲解搭档”。

我希望你基于当前整个项目代码，带我建立完整、清晰、可落地的项目认知。你的目标不是只做表面解释，而是帮助我真正看懂这个项目：它是干什么的、整体怎么跑、模块之间怎么协作、每个文件承担什么职责、涉及哪些核心知识点、实现里有哪些关键设计和注意事项。最终将内容输出到 docs/RAGDocs.md 中。 

请严格按下面的方式解析当前项目：

第一步，先建立全局观。  
先不要一上来陷入细节，而是先通读整个项目结构，告诉我：

1. 这个项目的核心目标是什么
2. 它解决了什么问题
3. 整体架构由哪些部分组成
4. 各模块之间是如何协作的
5. 项目的主链路是什么，从输入到输出是怎么流动的
6. 如果这是一个 RAG / Agent / Web 服务类项目，请明确区分：
   - 配置层
   - 数据接入层
   - 文档处理层
   - 检索层
   - 生成层
   - 编排层
   - Agent / Graph 层
   - API / 对外接口层

这一部分请优先帮我建立“脑图感”和“全局地图感”，而不是堆术语。

第二步，请结合项目代码阅读顺序，按分组带我理解项目。  
优先按下面的阅读思路进行，如果实际项目文件略有差异，请自动映射到最接近的模块：

第一组：边界与入口
- `config.py`
- `rag/__init__.py`
- `graph/state.py`

第二组：知识如何进库
- `rag/metadata.py`
- `rag/loaders.py`
- `rag/cleaner.py`
- `rag/splitter.py`
- `rag/embeddings.py`
- `rag/vector_store.py`
- `rag/bm25_store.py`
- `rag/indexer.py`

第三组：问题如何变成检索
- `rag/prompts.py`
- `rag/query_analyzer.py`
- `rag/retriever.py`
- `rag/reranker.py`

第四组：答案如何生成并接入主 Agent
- `rag/generator.py`
- `rag/service.py`
- `graph/nodes.py`
- `graph/agent.py`
- `main.py`

每进入一组时，请先告诉我：
1. 这一组解决的核心问题是什么
2. 为什么这一组应该放在这个阶段看
3. 这一组和上一组、下一组的关系是什么
4. 这一组涉及哪些重要知识点

第三步，逐文件详细讲解。  
对每一个关键文件，请按统一格式输出，尽量讲透，不要只写一两句：

1. 文件的核心职责
2. 它在整个项目中的位置
3. 这个文件主要解决什么问题
4. 重要类、函数、数据结构分别是做什么的
5. 核心执行流程是什么
6. 输入是什么，输出是什么
7. 它依赖了哪些其它模块
8. 它会被哪些模块调用
9. 这个文件涉及到哪些知识点
10. 阅读这个文件时最应该注意什么
11. 这个文件最容易让人混淆的点是什么
12. 如果要改这个文件，最容易踩的坑是什么

如果某个文件比较复杂，请拆成“小节”解释，不要一句带过。

第四步，遇到复杂概念、关键流程、模块关系时，请主动使用 Mermaid。  
当你判断某个内容用图比用文字更清楚时，直接输出最适合的 Mermaid 图，不需要等我提醒。比如：

- 项目总体架构图：用 `flowchart`
- 请求处理链路：用 `sequenceDiagram`
- RAG 建库流程：用 `flowchart`
- 检索与生成调用关系：用 `sequenceDiagram` 或 `flowchart`
- 状态流转 / LangGraph 节点关系：用 `stateDiagram-v2` 或 `flowchart`
- 模块依赖关系：用 `graph LR`

图不要为了画而画，重点是帮助我真正建立认知。  
每张图下面请配一小段解释，说明我应该重点关注什么。

第五步，不要只讲“是什么”，还要讲“为什么这样设计”。  
对重要实现，请补充解释：

1. 为什么这里要这样拆分模块
2. 为什么不是另一种更简单的写法
3. 这样设计的收益是什么
4. 它的局限和代价是什么
5. 在真实工程里，这种写法有什么维护价值

第六步，主动指出注意点和隐性风险。  
在讲解过程中，如果你发现这些内容，请主动提醒我：

- 容易误解的概念
- 容易忽略的依赖关系
- 配置项之间的联动影响
- 数据结构设计的关键前提
- 检索链路中的召回、过滤、重排风险
- Agent 编排中的状态流转问题
- API 层与内部服务层可能的耦合点
- 后续扩展时最该留意的改造点

第七步，讲解风格要求。  
请保持“带我读项目”的风格，而不是百科式解释。要求：

- 先讲结论，再展开细节
- 讲清文件“存在的意义”
- 尽量把抽象概念落到当前项目代码
- 不要只复述代码表面逻辑，要帮我建立工程理解
- 遇到术语时，优先结合当前文件上下文解释
- 避免泛泛而谈，尽量围绕当前项目真实实现来讲

第八步，输出节奏要求。  
请不要一次性把所有文件糊给我。请按“全局观 -> 分组 -> 逐文件”的顺序推进。  
每讲完一组，先做一个小结，告诉我：

1. 我已经理解了什么
2. 接下来该看什么
3. 当前最关键的认知抓手是什么

如果项目里存在和上述文件名不同但职责相近的文件，请自动识别并替换，不要因为文件名不完全一致就停止。

最后，请始终围绕一个目标：  
不是为了“解释代码”，而是为了让我建立这个项目的全局观、模块观、流程观、文件职责观，以及后续能够独立维护和扩展它的能力。

补充要求：
当你解释完一个文件后，请额外补充一句：
“如果我是第一次接触这个文件，我最应该先抓住哪 1-2 个关键点？”
并且如果这个文件和其它文件有强依赖，请明确提示我下一步最适合看的文件。